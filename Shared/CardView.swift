import SwiftUI

struct CardView: View {
    var body: some View {
       Text("Hello")
           
        }
    
}


struct CardView_Previews: PreviewProvider {
    static var previews: some View {
        Group {
            CardView()
        }
    }
}
